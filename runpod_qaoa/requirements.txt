# QAOA for QS/QV Prediction - Requirements
# Dr. Jack Hammer - QMLE Director
# RunPod H200 GPU Configuration

# Core quantum computing
pennylane>=0.34.0
pennylane-lightning>=0.34.0

# GPU-accelerated backends
pennylane-lightning-gpu>=0.34.0      # State vector GPU (cuStateVec)
# pennylane-lightning-tensor         # Tensor network GPU (cuTensorNet) - install separately

# Tensor network CPU fallback
quimb>=1.8.0
cotengra>=0.6.0

# CUDA/cuQuantum (typically pre-installed on RunPod GPU instances)
# cuquantum-python>=24.03.0
# cutensornet-cu12                   # For lightning.tensor GPU support

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Visualization (optional, for notebook)
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter (for notebook execution)
jupyter>=1.0.0
ipykernel>=6.25.0

# Utilities
tqdm>=4.65.0
